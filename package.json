{
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cypress": "^13.8.1",
    "cypress-cucumber-preprocessor": "^4.3.1",
    "wait-on": "^7.2.0"
  },
  "scripts": {
    "test": "wait-on http://localhost:8080 && cypress run"
  }
}
